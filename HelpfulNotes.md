# Python-0-Starting 詳細ガイド

## 最初に知っておきたい言葉

### 値と型

値は、プログラムが扱う具体的なデータです。例えば`42`、`"Hello"`、`False`などです。

型は、その値の種類です。

- `int`: 整数
- `float`: 小数を扱う数値
- `str`: 文字列
- `bool`: `True`または`False`という真偽値
- `list`: 複数の値を順番に入れるリスト
- `tuple`: 後から内容を変更できない並び
- `set`: 重複しない値の集まり
- `dict`: キーと値を対応付ける辞書

同じように見える値でも、型が違えば意味や扱い方が変わります。`0`は整数ですが、`"0"`は文字です。

### 変数

変数は、値に付ける名前です。

```python
number = 42
```

これは「`42`という値を`number`という名前で扱う」という意味です。数学の等号というより、右側の値を左側の名前へ入れる操作です。

### 関数

関数は、処理をひとまとまりにして名前を付けたものです。

```python
def greet(name):
    print(f"Hello, {name}")
```

`name`は関数が受け取る値で、引数と呼びます。`greet("Tokyo")`のように呼び出すと処理が実行されます。

### 表示と戻り値

`print()`は人間が読めるように画面へ表示します。`return`は関数の結果を呼び出し元へ返します。この2つは別のものです。

```python
def answer():
    print("計算しました")
    return 42
```

表示はメッセージ、戻り値は後続の処理で使えるデータ、と考えると分かりやすくなります。

### モジュールとパッケージ

Pythonファイル1つが基本的なモジュールです。複数のモジュールをディレクトリにまとめ、外部から再利用できる形にしたものがパッケージです。

### テストコード

テストコードは、実装が期待どおり動くかを自動確認するコードです。このプロジェクトでは主に`assert`を使っています。

```python
assert 1 + 1 == 2
```

条件が正しければ何も起きません。間違っていれば`AssertionError`が発生し、問題を発見できます。

## プロジェクト全体の流れ

| Exercise | 主題 | 身につく考え方 |
| --- | --- | --- |
| ex00 | 基本的なデータ構造 | データの持ち方と変更方法 |
| ex01 | 日時の取得と整形 | 標準ライブラリと表示形式 |
| ex02 | 最初の関数 | 引数、型判定、戻り値 |
| ex03 | Nullに見える値 | 値と型の厳密な違い |
| ex04 | 偶数・奇数判定 | コマンドライン引数と例外処理 |
| ex05 | 文字の集計 | 文字列分析と独立プログラムの構成 |
| ex06 | filterの再実装 | 条件を関数として渡す考え方 |
| ex07 | モールス変換 | 辞書による対応表と入力検証 |
| ex08 | プログレスバー | ジェネレーターと逐次処理 |
| ex09 | パッケージ作成 | 配布、ビルド、インストール |

ex05以降では、`main()`、docstring、例外処理、flake8による書式確認という追加ルールがあります。これは、単に動くだけでなく、他人が安全に読んで使えるプログラムへ移行する段階だと考えられます。

## PDFに書かれているプロジェクト共通ルール

この文書はリポジトリ内の[en.subject.pdf](./en.subject.pdf)と現在の実装を照合しています。PDFには各Exercise固有の指示に加えて、次の共通ルールがあります。

- Python 3.10を使用する。
- 課題で明示的に禁止されていなければ、Pythonの組み込み関数は使用できる。
- ライブラリのimportは明示的に書き、`from package import *`は使わない。
- グローバル変数は使用しない。
- プログラムを予期しない例外やクラッシュで終了させない。
- 採点対象はGitリポジトリに入っているファイルだけである。
- 提出ディレクトリ名と提出ファイル名は、大文字・小文字も含めて指定どおりにする。

PDFのChapter VIIは、ex04の後に「これ以降」の追加ルールを定めています。したがって、ex05からex09には次も適用されます。

- 実行処理をグローバルスコープへ直接書かず、関数へ入れる。
- 各プログラムに`main()`を用意する。
- `if __name__ == "__main__":`から`main()`を呼ぶ。
- 想定されたエラーも含め、例外を未処理のまま外へ出さない。
- すべての関数にdocstringを付ける。
- flake8に従う。

### 「Allowed functions: None」の読み方

PDFの`Allowed functions`は、そのExerciseで追加利用を許されたライブラリや機能を示します。`None`は「何も実装できない」という意味ではなく、一般ルールで許されている組み込み機能の範囲で解く、という意味です。例えば`print()`、`type()`、基本的な演算は課題を成立させるために利用します。

### PDFの出力にある`$`の意味

PDFでは`python file.py | cat -e`という確認例があります。行末に表示される`$`は`cat -e`が改行位置を見せるために付けた記号で、プログラム自身が出力する文字ではありません。

---

## ex00: First Python Script

対象ファイルは`ex00/Hello.py`です。

### PDFが要求していること

| 項目 | PDFの指定 |
| --- | --- |
| 提出先 | `ex00/` |
| 提出ファイル | `Hello.py` |
| Allowed functions | None |
| 主な要求 | list、tuple、set、dict内の文字列をキャンパスに対応した挨拶へ変更する |
| 出力順 | list、tuple、set、dictの順 |

PDFの見本はフランスのキャンパスを想定して、`World!`、`France!`、`Paris!`、`42Paris!`を表示しています。Tokyoキャンパスなら、国・都市・キャンパス名をそれぞれ`Japan!`、`Tokyo!`、`42Tokyo!`へ読み替えるのが意図です。

### PDFと現在の実装の照合

- listは`World!`になっており、要求に対応しています。
- tupleはTokyoキャンパスの国名として`Japan!`になっています。
- setは都市名として`Tokyo!`になっています。
- dictは現在`42 Tokyo!`で、`42`と`Tokyo`の間に空白があります。PDFの命名パターン`42Paris!`に合わせるなら`42Tokyo!`である必要があります。
- したがって、現在のex00は考え方は合っていますが、dictの空白1文字は提出前に課題のキャンパス表記と照合すべき差異です。

### 何をするExerciseか

Pythonの代表的な4種類のデータ構造を作り、中身を書き換えて表示します。同じ「複数の値を持つもの」でも、変更方法や性質が違うことを体験するExerciseです。

### 処理の流れ

最初に4種類のデータを作っています。

```python
ft_list = ["Hello", "tata!"]
ft_tuple = ("Hello", "toto!")
ft_set = {"Hello", "tutu!"}
ft_dict = {"Hello": "titi!"}
```

その後、それぞれに合った方法で内容を変更し、最後に`print()`で表示します。

### リストの書き換え

```python
ft_list[1] = "World!"
```

角括弧`[]`で作るリストは、後から中身を変更できます。`[1]`は2番目の要素を意味します。Pythonでは位置を0から数えるため、最初が`[0]`、次が`[1]`です。

この書き方を使う理由は、リストの一部分だけを変更できるからです。リスト全体を作り直す必要がありません。

### タプルの置き換え

```python
ft_tuple = ("Hello", "Japan!")
```

丸括弧`()`で作るタプルは、作成後に個々の要素を変更できません。そのため、2番目だけを書き換えるのではなく、新しいタプル全体を変数へ入れ直しています。

タプルは「後から変わってほしくない一組の値」を表現するときに役立ちます。

### セットの変更

```python
ft_set.remove("tutu!")
ft_set.add("Tokyo!")
```

セットは順番を管理せず、同じ値を重複して持たない集合です。位置番号で要素を指定できないため、古い値を`remove()`で削除し、新しい値を`add()`で追加します。

### 辞書の変更

```python
ft_dict["Hello"] = "42 Tokyo!"
```

辞書は、キーと値の対応表です。ここでは`"Hello"`がキーで、`"titi!"`が値です。位置ではなくキーを指定して値を書き換えます。

### なぜ4種類もあるのか

データの目的によって、適切な入れ物が異なるからです。

- 順番があり、後から変更したいならリスト
- 順番があり、変更させたくないならタプル
- 重複をなくし、順番を気にしないならセット
- 名前と値を対応付けたいなら辞書

### 落とし穴

- Pythonの位置番号は0から始まります。`[1]`は1番目ではなく2番目です。
- タプルの要素を`ft_tuple[1] = ...`のように変更するとエラーになります。
- セットの表示順は実行ごとに変わる可能性があります。順番が違っても内容が同じなら問題ありません。
- `remove()`で存在しない値を消そうとするとエラーになります。
- 出力文字列は空白も含めて評価されます。現在の実装は`"42 Tokyo!"`ですが、課題文が`"42Tokyo!"`を要求している版では空白1文字が不一致になります。提出前に手元の課題文と照合が必要です。
- 現在の辞書定義には`"Hello" :`のようにコロン前の空白があります。Pythonは実行できますが、flake8では通常`"Hello":`が推奨されます。

### このExerciseで学べること

データはすべて同じ方法で扱うのではなく、目的に合う構造を選ぶ必要があることを学びます。また、「変更可能なもの」と「変更不可能なもの」の違いは、今後のPython全体で重要になります。

---

## ex01: First Use of Package

対象ファイルは`ex01/format_ft_time.py`です。

### PDFが要求していること

| 項目 | PDFの指定 |
| --- | --- |
| 提出先 | `ex01/` |
| 提出ファイル | `format_ft_time.py` |
| Allowed functions | `time`、`datetime`、または日付取得用ライブラリ |
| 主な要求 | Unix時間をカンマ区切り・小数4桁と科学表記で表示し、日付を英語月名・日・年で表示する |

PDFの数値と日付は見本であり、実行時点の値と一致する必要はありません。重要なのは`Seconds since January 1, 1970:`という文章、数値の形式、`or`以降の科学表記、次行の日付形式です。

### PDFと現在の実装の照合

- `time.time()`でPDFが求めるUnix時間を取得しています。
- `:,.4f`でカンマ区切り・小数点以下4桁になっています。
- `:.2e`で小数部2桁の科学表記になっています。
- `%b %d %Y`で`Oct 21 2022`形式の日付になります。
- 使用ライブラリもPDFの許可範囲です。

### 何をするExerciseか

現在時刻を取得し、Unix時間と読みやすい日付の2形式で表示します。Pythonに最初から付属する標準ライブラリを使う練習です。

### `import`の意味

```python
import time
import datetime
```

`import`は、別のモジュールに用意された機能をこのファイルで使えるようにする命令です。日時計算を一から作らず、Pythonが提供する信頼できる機能を利用します。

### Unix時間の取得

```python
seconds = time.time()
```

`time.time()`は、1970年1月1日0時0分0秒から現在までに経過した秒数を返します。小数部分は1秒未満の時間です。

Unix時間は人間には読みにくい一方、コンピューター同士で時刻を比較・保存するのに便利です。

### 現在日時の取得

```python
current_date = datetime.datetime.now()
```

最初の`datetime`はモジュール名、2番目の`datetime`はその中のクラス名です。`now()`が現在の日時を作ります。

### f文字列と数値の書式

```python
f"{seconds:,.4f}"
```

先頭の`f`は、文字列の中へ変数を埋め込めるf文字列を表します。

- `,`は3桁ごとのカンマ
- `.4f`は小数点以下4桁の固定小数点表記

```python
f"{seconds:.2e}"
```

`.2e`は科学表記です。例えば`1.79e+09`は、おおよそ`1.79 × 10の9乗`を意味します。

### 日付の書式

```python
current_date.strftime("%b %d %Y")
```

`strftime()`は日時を指定形式の文字列へ変換します。

- `%b`: 英語の短い月名
- `%d`: 2桁の日
- `%Y`: 4桁の西暦

### 落とし穴

- 実行のたびに時刻が変わるため、見本と数値が完全一致することはありません。形式が一致しているかを確認します。
- タイムゾーンや実行時刻によって日付が変わります。
- `time.time()`と`datetime.now()`は別々の瞬間に実行されるため、日付が切り替わる瞬間にはごく小さなずれがあり得ます。
- `%m`を使うと月が数字になり、課題指定の英語月名にはなりません。
- 現在のコードは字下げにタブを使っています。ex01では追加規約前ですが、一般にはスペース4個へ統一すると読みやすくなります。

### このExerciseで学べること

既存ライブラリの機能を呼び出す方法、数値や日付を用途に合わせて見せる方法を学びます。データそのものと、人間に見せる表示形式は分けて考える必要があります。

---

## ex02: First Python Function

対象ファイルは`ex02/find_ft_type.py`、確認用コードは`ex02/tester.py`です。

### PDFが要求していること

| 項目 | PDFの指定 |
| --- | --- |
| 提出先 | `ex02/` |
| 提出ファイル | `find_ft_type.py` |
| Allowed functions | None |
| 必須プロトタイプ | `def all_thing_is_obj(object: any) -> int:` |
| 主な要求 | オブジェクトの型を指定形式で表示し、42を返す |
| 単体実行 | 何も表示しない |

PDFはlist、tuple、set、dict、`Brian`、`Toto`、整数10を使うtesterも提示しています。最後の整数では`Type not found`を表示し、戻り値`42`も表示することを求めています。

### PDFと現在の実装の照合

- 関数名、引数名、注釈、戻り値はPDFのプロトタイプどおりです。
- PDF指定の7ケースすべてに対応しています。
- 表示文字列の大文字・小文字と空白も期待出力に合っています。
- `find_ft_type.py`には関数定義しかないため、単体実行しても何も表示しません。
- `tester.py`は確認用で、PDF上の提出対象は`find_ft_type.py`だけです。

### 何をするExerciseか

渡された値の型を調べ、型に応じたメッセージを表示し、最後に必ず`42`を返します。関数、引数、条件分岐、戻り値をまとめて学びます。

### 関数定義の読み方

```python
def all_thing_is_obj(object: any) -> int:
```

- `def`は関数を定義する命令
- `all_thing_is_obj`は関数名
- `object`は受け取る引数名
- `: any`は受け取る値についての注釈
- `-> int`は整数を返すという注釈
- 末尾の`:`は、次の字下げ部分が関数本体であることを示す

注釈は通常、実行時に型を強制するものではありません。人間や解析ツールへの説明です。また、課題指定は小文字の`any`ですが、一般的な型注釈では`typing.Any`を使います。小文字の`any`はPythonの組み込み関数なので、実務コードでは注意が必要です。

### 型を取得する

```python
object_type = type(object)
```

`type()`は値の型を返します。一度変数へ保存することで、同じ型取得を何度も書かずに済みます。

### 型名を辞書で対応付ける

```python
type_names = {
    list: "List",
    tuple: "Tuple",
    set: "Set",
    dict: "Dict",
}
```

型そのものをキー、表示用の名前を値にしています。型ごとに似た`if`を4回書く代わりに、辞書から表示名を取り出せます。

### 条件分岐

```python
if object_type in type_names:
```

`in`は辞書のキーに対象が含まれているかを確認します。

```python
print(f"{type_names[object_type]} : {object_type}")
```

`type_names[object_type]`で表示名を取り出し、f文字列へ埋め込みます。

文字列だけは、文字列の内容もメッセージに含めるため、別の`elif`で扱います。それ以外は`else`で`Type not found`と表示します。

### `return 42`の意味

どの分岐を通っても、最後に`42`を呼び出し元へ返します。関数内で表示したメッセージとは別の結果です。

```python
print(all_thing_is_obj(10))
```

この場合、関数内で`Type not found`が表示された後、戻り値の`42`が外側の`print()`で表示されます。

### テスターの役割

`tester.py`は、リスト、タプル、セット、辞書、文字列、整数を順番に渡し、すべての分岐を通します。対象の関数を`import`しているため、`find_ft_type.py`単体では何も表示されないことも確認できます。

### 落とし穴

- `print()`と`return`を混同しないこと。表示されても戻り値があるとは限りません。
- `type(object) is list`のような厳密判定は、listを継承した独自型をlistとして扱いません。今回の課題には適していますが、実務では`isinstance()`を選ぶ場合があります。
- 引数名`object`は組み込みの`object`という名前を隠します。課題指定に従っていますが、自由に命名できる場合は`value`などが分かりやすい名前です。
- ファイルの末尾で関数を勝手に呼び出すと、importしただけで出力されて課題要件に反します。

### このExerciseで学べること

「入力を受け取る」「状態を調べる」「条件によって処理を変える」「結果を返す」という、ほぼすべてのプログラムに共通する基本形を学びます。

---

## ex03: NULL Not Found

対象ファイルは`ex03/NULL_not_found.py`、確認用コードは`ex03/tester.py`です。

### PDFが要求していること

| 項目 | PDFの指定 |
| --- | --- |
| 提出先 | `ex03/` |
| 提出ファイル | `NULL_not_found.py` |
| Allowed functions | None |
| 必須プロトタイプ | `def NULL_not_found(object: any) -> int:` |
| 主な要求 | すべてのNull系の値と型を表示し、成功なら0、対象外なら1を返す |
| 単体実行 | 何も表示しない |

PDFのtesterは`None`、`float("NaN")`、整数0、空文字列、`False`、通常文字列`Brian`を順番に渡します。表示名は変数名と同じではなく、NaNは`Cheese`と表示する指定です。

### PDFと現在の実装の照合

- PDFの5種類を型まで区別して表示します。
- 成功時は0、`Brian`のような対象外では`Type not Found`と表示して1を返します。
- `Empty:`の行では空文字自体を表示せず、型だけを表示するPDF形式に合っています。
- ファイル単体では関数を呼び出していないため、何も表示しません。
- 確認用`tester.py`は提出対象ではありません。

### 何をするExerciseか

Pythonで「空」「無い」「偽」に見える複数の値を区別して表示します。対象なら`0`、対象外なら`1`を返します。

扱う値は次の5つです。

- `None`: 値が存在しないことを表す専用値
- `NaN`: 数値ではないことを表す浮動小数点値
- `0`: 整数のゼロ
- `""`: 文字数0の空文字列
- `False`: 偽を表す真偽値

### `None`を`is`で調べる理由

```python
if object is None:
```

`None`は専用の単一オブジェクトなので、値の等しさを調べる`==`より、同じオブジェクトかを調べる`is`を使うのがPythonの標準的な書き方です。

### `NaN`の特殊な判定

```python
elif object_type is float and object != object:
```

`NaN`は特殊で、自分自身とも等しくありません。

```python
nan = float("NaN")
nan == nan  # False
```

この性質を使って判定しています。先に型が`float`か確認することで、別の型の特殊な比較動作に巻き込まれるのを防いでいます。

### `0`と`False`を型まで確認する理由

Pythonでは次が真になります。

```python
False == 0  # True
```

そのため、値が0かだけを調べると`False`まで整数のゼロとして扱う危険があります。

```python
elif object_type is int and object == 0:
```

型が整数で、かつ値が0という2条件で判別しています。`False`は最後に`object is False`で別扱いします。

### 空文字列

```python
elif object_type is str and object == "":
```

空文字列は`None`ではありません。「文字列という入れ物はあるが、中に文字がない」状態です。

### 戻り値0と1

対象値を認識できれば`0`、できなければ`1`です。コマンドラインプログラムでは0を成功、0以外を失敗として扱う慣習があり、その考え方に近い設計です。

### 落とし穴

- `if not object:`だけでは5種類を区別できません。すべて偽として評価される可能性があります。
- `NaN == NaN`は真になりません。
- `bool`は`int`と深い関係があり、`False == 0`、`True == 1`です。判定順と型確認が重要です。
- `0.0`は浮動小数点のゼロで、現在の実装では整数の`Zero`には該当しません。
- `"0"`は文字列であり、整数0ではありません。
- `Type not Found`は大文字・小文字まで課題指定に合わせる必要があります。

### このExerciseで学べること

見た目や真偽判定が似ていても、データの意味と型は異なることを学びます。データ分析では欠損値や異常値を扱うため、この区別は特に重要です。

---

## ex04: The Even and the Odd

対象ファイルは`ex04/whatis.py`です。

### PDFが要求していること

| 項目 | PDFの指定 |
| --- | --- |
| 提出先 | `ex04/` |
| 提出ファイル | `whatis.py` |
| Allowed functions | `sys`、または引数取得用ライブラリ |
| 主な要求 | 整数引数1つの偶奇を表示する |
| エラー | 整数でない、または引数が複数なら指定のAssertionErrorを表示する |

PDFは`14`、`-5`、引数なし、`0`、`Hi!`、`13 5`の6パターンを例示しています。引数なしでは何も表示しないことも明示されています。

### PDFと現在の実装の照合

- `sys.argv`だけを使って引数を受け取っています。
- 14と0はEven、-5はOddになります。
- 引数なしでは`return`し、何も表示しません。
- 整数変換失敗と複数引数で、PDFどおり異なるメッセージを表示します。
- 想定例外は`except`で処理され、トレースバックを出しません。
- Chapter VIIの追加ルールはPDF上でex04の次に登場するため、厳密にはex05以降の規則です。ただし現在のex04も先取りして`main()`とdocstringを使っています。

### 何をするExerciseか

コマンドラインから整数を1つ受け取り、偶数なら`I'm Even.`、奇数なら`I'm Odd.`と表示します。不正な入力は指定されたエラーメッセージに変換します。

### `sys.argv`とは

```python
import sys
```

`sys`はPythonの実行環境に関する機能を提供する標準モジュールです。`sys.argv`には、コマンド実行時に入力された文字列がリストとして入ります。

```sh
python whatis.py 14
```

おおよそ次の状態になります。

```python
sys.argv == ["whatis.py", "14"]
```

最初の要素はスクリプト名なので、ユーザーの最初の入力は`sys.argv[1]`です。

### `main()`を使う理由

処理全体を`main()`へまとめると、ファイルを直接実行した場合だけ処理を開始し、別ファイルからimportしたときには勝手に動かない構成にできます。

```python
if __name__ == "__main__":
    main()
```

`__name__`はPythonが自動で用意する名前です。直接実行されたファイルでは`"__main__"`になります。

### 引数の個数確認

```python
assert len(sys.argv) <= 2, "more than one argument is provided"
```

スクリプト名を含めて2要素以下、つまりユーザー引数が1個以下であることを確認します。条件が偽なら`AssertionError`になります。

引数がなければ`return`で`main()`を終了し、何も表示しません。

### 整数への変換

```python
number = int(sys.argv[1])
```

コマンドライン引数は必ず文字列なので、計算前に`int()`で整数へ変換します。`"Hi!"`のように変換できない文字列なら`ValueError`が発生します。

### 偶数と奇数の判定

```python
if number % 2 == 0:
```

`%`は割り算の余りを求める演算子です。2で割った余りが0なら偶数、そうでなければ奇数です。負の整数でもこの判定は使えます。

### 例外処理

`try`内で起きる可能性のあるエラーを、`except`で種類別に受け止めます。これにより長いトレースバックを出さず、利用者へ分かりやすいメッセージを表示できます。

### 落とし穴

- `sys.argv`にはスクリプト名も含まれます。ユーザー引数1つなら長さは2です。
- `"14"`は文字列なので、そのままでは数値計算に使うべきではありません。
- `14.0`という入力は、人間には整数相当に見えても`int("14.0")`では変換できません。
- `% 2`は負の数にも使えます。`-5`は奇数です。
- `assert`は`python -O`という最適化モードでは無効になります。課題では指定動作を学ぶため使っていますが、実務の入力検証では明示的な`if`と`raise`の方が安全です。
- `main()`にはdocstringがありますが、戻り値注釈はありません。ex05以降の実装では`-> None`も付け、戻り値がないことを明確にしています。

### このExerciseで学べること

利用者から入力を受け取り、正しい形式か確認し、計算し、問題があれば安全に説明するというコマンドラインプログラムの基本を学びます。

---

## ex05: First Standalone Python Program

対象ファイルは`ex05/building.py`です。

### PDFが要求していること

| 項目 | PDFの指定 |
| --- | --- |
| 提出先 | `ex05/` |
| 提出ファイル | `building.py` |
| Allowed functions | `sys`、または引数取得用ライブラリ |
| 入力 | 文字列引数1つ。未指定または空なら入力を促す |
| 集計 | 大文字、小文字、句読点・記号、数字、空白、全文字数 |
| エラー | 引数が複数ならAssertionErrorを表示する |

PDFは171文字の長文例と、引数なしで`Hello World!`を入力する例を示しています。後者ではEnterの改行も空白として数えるため、12文字ではなく13文字、空白1個ではなく2個になります。PDFは「車復帰も空白として数える。入れたくなければCtrl+Dを使う」と補足しています。

また、PDFはここから`main()`、未処理例外禁止、全関数のdocstring、flake8を要求します。

### PDFと現在の実装の照合

- `main()`と実行ガードがあり、両関数にdocstringがあります。
- 各分類と全文字数をPDF指定順で表示します。
- `sys.stdin.read()`が改行を保持するため、PDFの13文字・空白2個という例に対応します。
- 複数引数のAssertionErrorは関数内で処理されます。
- flake8に通る形式です。
- `string.punctuation`を使っています。PDFのAllowed functions欄は引数取得用として`sys`を挙げる一方、本文は「言語機能を使い、車輪を再発明しない」と強調しています。この標準定義の利用は本文の意図に合いますが、Allowed functionsをimport制限として極端に厳格に読む評価環境では確認ポイントになります。

### 何をするExerciseか

文章を受け取り、全文字数、大文字、小文字、記号、空白、数字の数を表示します。入力、分析、出力、例外処理を備えた独立プログラムを作ります。

### 処理を2つの関数に分ける理由

- `display_character_counts(text)`: 文章を分析して表示する
- `main()`: 入力の受け取りとエラー処理を担当する

役割を分けると、分析処理だけを別の場所から再利用しやすくなり、問題が起きた場所も探しやすくなります。

### 型注釈

```python
def display_character_counts(text: str) -> None:
```

`text: str`は文字列を受け取る意図、`-> None`は値を返さず表示だけ行う意図を示します。

### 文字数の数え方

```python
upper_letters = sum(character.isupper() for character in text)
```

これは文章を1文字ずつ調べ、大文字なら`True`、違えば`False`を作ります。Pythonでは`True`を1、`False`を0として合計できるため、大文字の数になります。

同じ仕組みで次を調べます。

- `isupper()`: 大文字か
- `islower()`: 小文字か
- `isspace()`: 空白文字か
- `isdigit()`: 数字か

丸括弧内の`character.isupper() for character in text`はジェネレーター式です。判定結果を必要な分だけ順番に作るため、巨大な文章でも余分なリストを作らずに済みます。

### 記号の判定

```python
character in string.punctuation
```

`string.punctuation`には、ASCIIで一般的な句読点や記号がまとまっています。記号を手作業で列挙するより、標準ライブラリを使う方が読みやすく間違いも減ります。

### 引数なしの入力

```python
text = sys.stdin.read()
```

引数がない場合は標準入力をEOFまで読みます。macOSやLinuxの対話入力ではCtrl+DがEOFの操作です。

`input()`ではなく`sys.stdin.read()`を使う重要な理由は、改行を含めて文章全体を読めることです。課題ではEnterで入力された改行も空白1文字として数えます。

### docstring

関数直下の三重引用符で囲まれた文章はdocstringです。関数の目的を機械と人間の両方が参照できます。

```python
"""Display the character categories contained in text."""
```

### 落とし穴

- `sys.stdin.read()`はEOFまで待ち続けます。Enterだけでは終了せず、Ctrl+Dが必要です。
- 改行、タブ、通常の半角空白はすべて`isspace()`で空白として数えられます。
- `string.punctuation`はASCII記号が中心です。日本語の`。`や`、`は含まれません。
- 1文字が必ずどれか1分類だけになるとは限りません。Unicodeには大文字・小文字の分類が単純でない文字もあります。
- 複数の単語を1引数にするには、シェルで文章全体を引用符で囲む必要があります。
- `assert`による引数検証は最適化モードで無効になる点に注意が必要です。

### このExerciseで学べること

文章を小さな単位へ分解して集計する方法、標準入力、関数の役割分担、docstring、例外処理を学びます。データ分析の「入力データを分類して数える」という基本操作にもつながります。

---

## ex06: Reimplementing Filter

対象ファイルは`ex06/ft_filter.py`と`ex06/filterstring.py`、確認用コードは`ex06/tester.py`です。

### PDFが要求していること

| 項目 | PDFの指定 |
| --- | --- |
| 提出先 | `ex06/` |
| 提出ファイル | `ft_filter.py`、`filterstring.py` |
| Allowed functions | `sys`、または引数取得用ライブラリ |
| Part 1 | 組み込み`filter`を使わず、リスト内包表記で`ft_filter`を再実装する |
| Part 2 | 文字列Sと整数Nを受け、長さがNより大きい単語をリスト表示する |
| 必須表現 | 少なくとも1つのリスト内包表記と1つのlambda |
| エラー | 引数個数または型が不正なら`AssertionError: the arguments are bad` |

PDFは`Hello the World `と4から`['Hello', 'World']`、99から空リストを作る例を示します。引数の順序を逆にした場合と引数なしの場合はエラーです。

### PDFと現在の実装の照合

- `ft_filter.py`は組み込み`filter()`を呼ばず、リスト内包表記を使っています。
- `function=None`の場合も組み込みfilterの基本的な真偽選別に合わせています。
- docstringは`filter.__doc__`と完全一致するよう作られています。
- `filterstring.py`はlambdaを使い、PDFの正常・異常出力例に対応しています。
- 両提出ファイルを合わせると、リスト内包表記とlambdaの両方があります。
- 注意点として、PDFは組み込みfilterのように振る舞うことを要求しますが、現在の`ft_filter`はfilterオブジェクトではなくリストを返します。PDFが同時にリスト内包表記を要求しているため典型的な学習用解釈ではありますが、遅延評価や戻り値型まで厳密に比較する評価では差異になります。
- 「プログラム内に両表現」と狭く解釈して`filterstring.py`単体を静的確認する評価の場合、リスト内包表記が`ft_filter.py`側にある点も確認対象です。

### 何をするExerciseか

前半では、条件に合う要素だけを残す`filter`相当の関数を自作します。後半では文章と整数を受け取り、指定文字数より長い単語だけを表示します。

### `ft_filter`の役割

```python
def ft_filter(function, iterable):
```

`function`は「残すか捨てるかを決める条件」、`iterable`は順番に要素を取り出せるデータです。

```python
return [item for item in iterable if function(item)]
```

これはリスト内包表記です。通常のループで書くと「各要素を調べ、条件が真なら新しいリストへ追加する」という処理を、短く表現しています。

### `function is None`の意味

組み込み`filter`は条件関数に`None`を指定できます。その場合、各要素そのものを真偽判定します。

```python
ft_filter(None, [0, 1, "", "Python", False])
```

`0`、空文字、`False`は偽として除かれ、`[1, "Python"]`が残ります。

### 特殊なdocstring表記

`ft_filter`の先頭には、括弧でまとめた複数の文字列があります。

```python
(
    "filter(...)\n\n"
    "Return ..."
)
```

隣り合う文字列はPythonが自動的に連結します。1行を短く保ちながら、組み込み`filter.__doc__`と完全に同じdocstringを作るための書き方です。`\n`は改行を表します。

### `filterstring.py`の流れ

1. ユーザー引数が2つあるか確認する
2. 文章を`text`へ保存する
3. 第2引数を`int()`で整数へ変換する
4. `text.split()`で空白区切りの単語に分ける
5. `ft_filter`で長い単語だけを残す
6. 結果を表示する

### lambda式

```python
lambda word: len(word) > minimum_length
```

lambdaは、その場で作る名前のない短い関数です。「単語の長さが指定値より大きければ真」という条件を表します。

通常の関数で書くと次のような意味です。

```python
def is_long_word(word):
    return len(word) > minimum_length
```

一度しか使わない短い条件なのでlambdaが適しています。

### `split()`を使う理由

`text.split()`は連続する空白をまとめて区切りとして扱い、前後の余分な空白も除いて単語リストを作ります。課題の「単語は空白で区切られる」という条件に合っています。

### テスターの工夫

`subprocess.run()`は`filterstring.py`を別プロセスとして本当にコマンド実行します。関数だけでなく、引数の受け取り、標準出力、終了状態まで確認できます。

`capture_output=True`で出力を保存し、`text=True`で文字列として扱います。`check=False`は終了コードが0以外でもテスト側が自分で確認できる設定です。

### 落とし穴

- 条件は「`N`以上」ではなく「`N`より大きい」です。`>=`ではなく`>`を使います。
- `split()`後の句読点は単語の一部になります。ただし課題では特殊文字が含まれない前提です。
- 第2引数が整数に変換できなければ`ValueError`になります。実装はこれを指定メッセージへ変換しています。
- 現在の`ft_filter`はリストを返します。組み込み`filter()`は遅延評価するfilterオブジェクトを返すため、完全に同じではありません。課題がリスト内包表記を求めているため教育用に単純化された実装です。巨大データや無限データでは違いが重要になります。
- `lambda`は短い条件には便利ですが、複雑になると名前付き関数の方が読みやすくなります。
- `filter`という組み込み関数そのものを`ft_filter.py`内で使うと課題違反です。

### このExerciseで学べること

処理対象のデータと、選択条件を別々に扱う考え方を学びます。関数を値のように別の関数へ渡す「高階関数」は、データ処理で頻繁に使われます。

---

## ex07: Dictionaries SoS

対象ファイルは`ex07/sos.py`、確認用コードは`ex07/tester.py`です。

### PDFが要求していること

| 項目 | PDFの指定 |
| --- | --- |
| 提出先 | `ex07/` |
| 提出ファイル | `sos.py` |
| Allowed functions | `sys`、または引数取得用ライブラリ |
| 入力 | 空白と英数字からなる文字列1つ |
| 変換 | 英数字を`.`と`-`、空白を`/`へ変換する |
| 形式 | モールス文字同士を半角空白1つで区切る |
| 必須構造 | モールス対応を辞書へ保存する |
| エラー | 引数個数または文字種が不正なら指定AssertionError |

PDFは辞書名の例として`NESTED_MORSE`を示していますが、中心要件は辞書を使うことです。`sos`は`... --- ...`、`h$llo`はエラーになる例が示されています。

### PDFと現在の実装の照合

- `nested_morse`辞書に空白、A〜Z、0〜9の対応を保存しています。
- 小文字入力を大文字へ統一するため、PDFの`sos`例に対応します。
- 辞書にない`$`などを変換前に検出します。
- `join()`により、符号間の空白は1個、末尾の余分な空白はありません。
- 引数個数と不正文字の例外は`main()`内で処理します。
- 関数にはdocstringがあり、flake8形式です。
- 辞書名はPDF例の大文字`NESTED_MORSE`ではなく小文字`nested_morse`ですが、関数内のローカル変数であることに合うPythonの命名規則です。PDFが名前そのものを必須としているかは明記されていません。

### 何をするExerciseか

英数字と空白でできた文章をモールス信号へ変換します。文字とモールス符号の対応表には辞書を使います。

### 辞書が適している理由

```python
nested_morse = {
    "A": ".-",
    "B": "-...",
    " ": "/",
}
```

これは「Aなら`.−`」「Bなら`−...`」という対応表です。大量の`if`を並べるより、文字をキーとして直接符号を取り出す方が明確です。

辞書を関数内に置いているのは、プロジェクトのグローバル変数禁止ルールに合わせるためです。代わりに、関数を呼ぶたび辞書を作り直す費用があります。実務では変更されない定数をモジュールレベルへ置く設計も一般的ですが、この課題ではルールを優先しています。

### 入力を大文字へ統一する

```python
characters = text.upper()
```

辞書には大文字だけを登録しています。入力を大文字に統一すれば、小文字用の同じ対応を二重に登録せずに済みます。

### 全文字の検証

```python
all(character in nested_morse for character in characters)
```

各文字が辞書に存在するかを調べ、`all()`ですべて真か確認します。1文字でも`$`など未対応文字があれば偽になり、`AssertionError`になります。

単に`isalnum()`を使わず辞書のキーを確認することで、辞書にないUnicode英数字を誤って許可する問題も防げます。

### `join()`による連結

```python
return " ".join(
    nested_morse[character] for character in characters
)
```

各文字をモールス符号へ変換し、符号の間に半角空白を1つ入れて連結します。元の文章の空白は辞書によって`/`になります。

例えば`"A B"`は次の3要素になります。

```text
.- / -...
```

`join()`を使うと、末尾に不要な空白を付けず、要素間だけを正確に区切れます。

### 落とし穴

- モールス符号同士の区切りは半角空白1つです。末尾の余分な空白も評価で不一致になる可能性があります。
- 文章中の空白は`/`ですが、出力上では前後に区切り用空白が入るため`... / ---`のようになります。
- `$`、`!`、日本語など、辞書にない文字はエラーです。
- `.upper()`を行わないと小文字が辞書に見つかりません。
- `assert`は最適化モードで無効になります。実務では入力検証へ明示的な例外送出を使う方が堅牢です。
- 空文字列は全文字検証を通り、空行を表示します。空文字列をエラーにする要件なら追加確認が必要ですが、現在の課題文は文字列引数1つを中心に定義しています。

### このExerciseで学べること

対応表を辞書として表現する方法、入力を正規化する方法、すべての入力を事前検証してから変換する方法を学びます。この形は翻訳、コード変換、商品コード検索など多くの場面に応用できます。

---

## ex08: Loading

対象ファイルは`ex08/Loading.py`、確認用コードは`ex08/tester.py`です。

### PDFが要求していること

| 項目 | PDFの指定 |
| --- | --- |
| 提出先 | `ex08/` |
| 提出ファイル | `Loading.py` |
| Allowed functions | `os` |
| 必須プロトタイプ | `def ft_tqdm(lst: range) -> None:` |
| 主な要求 | `yield`を使い、tqdmにできるだけ近い進捗表示を作る |
| 幅調整 | `get_terminal_size`を利用可能 |

PDFのtesterは333要素を0.005秒ずつ処理し、自作版と本物の`tqdm`を続けて表示して比較します。自作版の期待形式は、百分率、角括弧付きバー、`333/333`です。

### PDFと現在の実装の照合

- 関数名とプロトタイプはPDFどおりです。
- `yield`でrangeの要素を元の順番のまま返します。
- 進捗率、バー、現在数と全体数を同一行へ表示します。
- 許可された`os.get_terminal_size()`で端末幅へ対応します。
- 非端末環境での`OSError`も処理するため、未処理例外を避けています。
- PDFの提出対象は`Loading.py`だけです。現在の`tester.py`は本物の`tqdm`との視覚比較ではなく、自作版の要素と表示を自動検証する補助テストです。
- 本物の`tqdm`が表示する経過時間、残り時間、処理速度までは実装していません。PDFの自作版期待行もそれらを含めていないため、提示形式には対応しています。

### 何をするExerciseか

`range`の要素を順番に渡しながら、処理の進捗を同じ行に表示する`ft_tqdm`を作ります。中心となる概念は`yield`とジェネレーターです。

### `yield`とは

通常の`return`は値を返すと関数が終了します。`yield`は値を1つ返して一時停止し、次の要素を求められたときに停止位置から再開します。

```python
for element in ft_tqdm(range(3)):
    do_something(element)
```

大まかな流れは次のとおりです。

1. `ft_tqdm`が要素を1つ`yield`する
2. 呼び出し側がその要素を処理する
3. `ft_tqdm`が再開し、完了分のバーを表示する
4. 次の要素を`yield`する

全要素を一度に新しいリストへコピーしないため、大量データの逐次処理に向いています。

### 空のrangeへの対応

```python
if total == 0:
    return
```

要素数0の場合はすぐ終了します。これがないと、進捗率計算で0除算が起きる可能性があります。

### 端末幅の取得

```python
terminal_width = os.get_terminal_size().columns
```

端末の横幅を取得し、バー全体が収まるようにします。自動テストやファイルへの出力など、端末ではない環境では`OSError`になる可能性があるため、80文字へフォールバックします。

### 進捗率

```python
percentage = int(current / total * 100)
```

現在数を全体数で割って割合を出し、100倍して百分率へ変換します。`int()`で小数部分を切り捨て、表示しやすい整数にします。

### 表示幅の計算

プログレスバー以外にも、百分率、括弧、現在数、全体数を表示する必要があります。

```python
bar_width = max(1, terminal_width - len(prefix) - len(suffix))
```

端末幅から前後の文字数を引き、残りをバーに使います。`max(1, ...)`は、端末が非常に狭くてもバー幅を最低1文字確保するためです。

### バーの組み立て

- `=`: 完了済み部分
- `>`: 現在位置
- 半角空白: 未完了部分

文字列に整数を掛けると、その回数だけ繰り返せます。

```python
"=" * 3  # "==="
```

この性質を使って、進捗に応じた長さのバーを作ります。

### `\r`、`end`、`flush`

```python
print(f"\r{prefix}{bar}{suffix}", end="", flush=True)
```

- `\r`: カーソルを同じ行の先頭へ戻す
- `end=""`: `print()`が自動で改行しないようにする
- `flush=True`: 出力をため込まずすぐ画面へ反映する

これにより、行を大量に増やさず、同じ場所のバーが動いて見えます。

### なぜ`main()`が空なのか

`Loading.py`は単独で処理を行うプログラムではなく、ほかのファイルから`ft_tqdm`をimportして使うモジュールです。それでも追加規約の形式を保つため、何もしない`main()`と`pass`を用意しています。

### テスターの工夫

`redirect_stdout()`で画面への出力を`StringIO`というメモリ上の文字列へ一時的に向けます。これにより、バーの表示回数、100%表示、最終カウンターを自動確認できます。

### 落とし穴

- `yield`を含む関数は、呼び出しただけでは中身が実行されません。ループや`list()`で要素を要求して初めて進みます。
- 型注釈は課題指定どおり`-> None`ですが、実際にはyieldするジェネレーターを返します。実務では`Iterator[int]`など、実態に合う注釈が適切です。
- バー自身は改行しません。利用側がループ後に`print()`して改行する必要があります。
- 処理が速すぎると、途中経過が目で見えないことがあります。見本では`sleep()`を使って意図的に遅くしています。
- `os.get_terminal_size()`は常に成功するとは限りません。
- `range(0)`のときは何も表示しません。
- 進捗表示は`yield`後に更新されるため、「呼び出し側の処理が1件終わった後」の進捗を示します。

### このExerciseで学べること

大量のデータを一度に抱えず、1件ずつ受け渡す逐次処理を学びます。ファイル読み込み、データ変換、ネットワーク処理など、時間のかかる仕事の進捗表示にもつながります。

---

## ex09: My First Package Creation

対象は`ex09`ディレクトリ全体です。主なファイルは`ft_package/__init__.py`、`pyproject.toml`、`README.md`、`LICENSE`です。

### PDFが要求していること

| 項目 | PDFの指定 |
| --- | --- |
| 提出先 | `ex09/` |
| 提出ファイル | `*.py`、`*.txt`、`*.toml`、`README.md`、`LICENSE` |
| Allowed functions | PyPI、またはパッケージ作成用ライブラリ |
| パッケージ名 | `ft_package` |
| 見本バージョン | `0.0.1` |
| 確認 | `pip list`に現れ、`pip show -v ft_package`で情報を表示できる |
| 配布形式 | sdistの`.tar.gz`とwheelの`.whl`の両方からインストールできる |
| 公開関数 | `from ft_package import count_in_list`が使える |

PDFは`count_in_list(["toto", "tata", "toto"], "toto")`が2、`"tutu"`が0になることを明示しています。作者`eagle`やURLなどの表示はパッケージ情報の見本であり、本文はパッケージを「自分の望む方法で」作成するとしています。

### PDFと現在の実装の照合

- パッケージ名`ft_package`、バージョン`0.0.1`、概要`A sample test package`は見本と一致します。
- 指定import文で`count_in_list`を利用でき、2と0の結果になります。
- `pyproject.toml`、パッケージREADME、MIT LICENSEがあります。
- sdistとwheelを実際にビルドし、それぞれ独立環境へインストールして動作確認済みです。
- 作者とURLはPDFのeagleではなく、このリポジトリの`smizuoch`情報を使っています。本文の自由度には合いますが、表示例との文字列完全一致ではありません。
- 最新のsetuptoolsで作るため、`pip show -v`の`Metadata-Version`はPDF例の2.1ではなく2.4、ライセンス表示は`License-Expression: MIT`になることがあります。これは新しいパッケージ仕様による表示差です。
- PDFの提出ファイル欄はソース系拡張子を挙げ、同時に`dist`内の2形式からインストールできることを求めています。現在の`.gitignore`では通常`dist/`がGit管理外になるため、clone後はREADMEのビルド手順を実行して配布物を生成する必要があります。評価が生成済みdist自体をGitに要求する運用なら、提出前に評価手順を確認する必要があります。

### 何をするExerciseか

自作した`count_in_list()`を、ほかの環境へインストールしてimportできるPythonパッケージにします。これまでのExerciseはリポジトリ内のスクリプトでしたが、ex09ではコードを配布可能な製品の形へ整えます。

### `count_in_list`の処理

```python
def count_in_list(values: list, target: object) -> int:
    return values.count(target)
```

`list.count()`は、リスト内に指定値と等しい要素が何個あるかを返します。

```python
count_in_list(["toto", "tata", "toto"], "toto")  # 2
```

すでにPythonにある機能を適切に使うことで、手動ループより短く、意図が明確になります。

`target: object`は、検索対象が文字列に限らず、整数など任意のPythonオブジェクトでよいことを示しています。

### `__init__.py`の役割

`__init__.py`があることで、`ft_package`ディレクトリをPythonパッケージとして扱えます。関数をこのファイルで公開しているため、利用者は内部ファイル構成を意識せず次のように書けます。

```python
from ft_package import count_in_list
```

### `pyproject.toml`の役割

`pyproject.toml`はパッケージの設計票です。

`[build-system]`では、ビルドに`setuptools`と`wheel`を使うことを指定します。

```toml
[build-system]
requires = ["setuptools>=77", "wheel"]
build-backend = "setuptools.build_meta"
```

`[project]`には利用者が`pip show`で確認できる情報を記載します。

- `name`: インストール名
- `version`: バージョン
- `description`: 概要
- `readme`: 詳細説明ファイル
- `requires-python`: 対応Pythonバージョン
- `license`: 利用条件
- `authors`: 作者情報

`[project.urls]`は関連Webサイト、`[tool.setuptools.packages.find]`は配布物に含めるパッケージの探索条件です。

### sdistとwheel

ビルドすると2種類の配布物が作られます。

#### sdist

```text
ft_package-0.0.1.tar.gz
```

ソースコードをまとめた配布物です。インストール先でビルド処理が必要です。中身を確認しやすく、異なる環境で再ビルドできます。

#### wheel

```text
ft_package-0.0.1-py3-none-any.whl
```

Python向けのインストール用パッケージ形式です。今回の`py3-none-any`は、Python 3用で、特定OSやCPUに依存しないことを示します。通常はsdistより速く簡単にインストールできます。

### READMEとLICENSE

パッケージ内の`README.md`は、目的、ビルド方法、インストール方法、使用例を利用者へ伝えます。

`LICENSE`は他人がコードをどの条件で使用・変更・再配布できるかを明示します。現在はMITライセンスです。

### ビルドとインストールの流れ

```sh
cd ex09
uv run --with build python -m build
```

この操作で`dist`にsdistとwheelが作られます。その後、どちらかを`pip install`できます。

```sh
pip install ./dist/ft_package-0.0.1-py3-none-any.whl
pip show -v ft_package
```

`pip show -v`は名前、バージョン、説明、作者、ライセンス、インストール場所などを表示します。

### テスターの役割

`ex09/tester.py`は、対象値が複数ある場合、存在しない場合、文字列以外の場合を確認します。パッケージの計算機能を確認するテストです。

このプロジェクトではさらに、sdistとwheelを別々の独立環境へインストールし、インストール後のimportと実行も確認しています。ソースディレクトリで直接動くだけでは、パッケージが正しく作れた証明にならないためです。

### 落とし穴

- パッケージ名、バージョン、生成ファイル名が課題指定と一致する必要があります。
- `__init__.py`で関数を公開しないと、指定されたimport文が使えない場合があります。
- ソース上で動いても、配布物に必要ファイルが入っているとは限りません。クリーンな仮想環境へのインストール確認が重要です。
- `dist`や`build`は一般的な`.gitignore`で除外されることがあります。新しくcloneした環境では、インストール前に再ビルドが必要です。
- コードを変更したら配布物も再ビルドしないと、古いコードがwheelに残ります。
- sdistとwheelは別物なので、両方を確認する必要があります。
- バージョンを変更せずに異なる内容を配布すると、利用者が更新を判別できません。
- `list.count()`は`==`による等価比較を使います。例えばPythonでは`1 == True`が真なので、型まで厳密に区別したい用途では追加設計が必要です。
- 作者のメールアドレスは配布物の公開情報になります。公開先に応じて記載内容を確認する必要があります。

### このExerciseで学べること

コードを書くことと、他人が安全に取得・インストール・利用できる形にすることは別の仕事だと学びます。パッケージ構成、メタデータ、ライセンス、バージョン、ビルド、独立環境での検証という、ソフトウェア配布の基礎がまとまっています。

---

## このプロジェクト全体で学べること

このプロジェクトは、次の順番でPythonプログラムの範囲を広げています。

1. 値を作り、変更し、表示する
2. 標準ライブラリを利用する
3. 処理を関数にまとめる
4. 値と型を正確に区別する
5. 外部入力を検証し、例外を処理する
6. 文章やリストを条件に従って分析する
7. 辞書を使ってデータを変換する
8. データを1件ずつ処理する
9. 作った機能を配布可能なパッケージにする

技術的な個別機能だけでなく、次の習慣も重要です。

- 期待する入力と出力を明確にする
- 正常な場合だけでなく、不正入力もテストする
- 人間が読める名前とdocstringを付ける
- 同じ処理を何度も書かず、標準機能を活用する
- 例外を利用者に分かる形で処理する
- importしただけで勝手に処理が走らない構成にする
- flake8で一定の読みやすさを維持する
- 自分の環境だけでなく、独立した環境でも確認する

最終的に学ぶのは、Pythonの文法だけではありません。「データを正しく理解し、処理を小さく分け、失敗を想定し、他人が使える形で届ける」というプログラミング全体の考え方です。

## 実行・確認コマンド一覧

```sh
uv run python ex00/Hello.py
uv run python ex01/format_ft_time.py
uv run python ex02/tester.py
uv run python ex03/tester.py
uv run python ex04/whatis.py 14
uv run python ex05/building.py "Hello World! 42"
uv run python ex06/tester.py
uv run python ex07/tester.py
uv run python ex08/tester.py
uv run python ex09/tester.py
```

ex09の配布物を作る場合は次を実行します。

```sh
cd ex09
uv run --with build python -m build
```

各コマンドの出力は、文字の大小、空白、句読点、改行まで含めて課題文と比較してください。プログラムは「意味が近い」だけでなく、指定された形式と正確に一致することが評価では重要です。
